# Let D be data frame.
# sapply(D, is.special)  #go through dataframe columns; if column is numeric, then check if it's a special character or NA. FALSE values mean that data are fine.
#is.special <- function(x){
#  if (is.numeric(x) !is.finite(x) else is.na(x)) # if it's numeric, 
#}

